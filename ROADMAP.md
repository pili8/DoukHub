## DoukHub 开发路线

### 一期（当前 ✅ 已完成）

以本地功能为主，跑通四步同步流程：

1. **导入分享表** — 文本 → 分享表 ✅
2. **解析分享表** — 分享码 → sec_user_id（调 TTD API） ✅
3. **同步账号表** — 分享表 → 账号表 + 拉详情（调 TTD API） ✅
4. **更新账号表** — 对未获取信息的账号重新拉详情 ✅

其他已完成功能：
- 单作品采集（视频/图集/实况/动图，SSE 进度，下载历史）
- 抖音直连解析（ABogus 签名，绕过 TTD）
- 批量采集（TTD 终端模式，批次管理）
- 采集预设与存储方案
- 数据库管理（表浏览、搜索、排序、批量操作、行内编辑）
- 数据备份与恢复（VACUUM INTO，自动备份）
- 文件查重（三级过滤，回收区）
- API 请求模式（外部设备调用，X-API-Key 鉴权）
- 后台任务队列（串行，页面切换不影响执行）
- 移动端适配

### 二期（规划中）

飞书双向同步：

- 本地数据跑完整后同步到飞书（本地 → 飞书）
- 飞书上的更改也要反映回本地（飞书 → 本地）

**背景**：之前飞书同步效果不佳，改为"本地优先"思路。先跑通本地，二期再做双向同步。

**注意**：系统字段中 record_id / synced / local_updated_at 原为飞书同步设计，一期保留不用，二期启用。

### 设计原则

- 分享表的「解析状态」字段（待解析/已就绪/解析失败），仅作为步骤2解析成功的动作反馈
- 账号表的「获取状态」字段（待获取/已获取/获取失败），表示步骤3/4是否成功拉取详情
- 不额外加「是否已入账号表」字段，靠 sec_user_id 关联 + 账号表「获取状态」反查
- 步骤4天然承担步骤3失败记录的重试通道
