## DoukHub 开发路线

### 一期（当前）

以本地功能为主，跑通四步同步流程：

1. **导入采集表** — 文本 → 采集表
2. **解析采集表** — 分享码 → sec_user_id（调 TTD API）
3. **同步账号表** — 采集表 → 账号表 + 拉详情（调 TTD API）
4. **更新账号表** — 对未获取信息的账号重新拉详情

### 二期（规划中）

飞书双向同步：

- 本地数据跑完整后同步到飞书（本地 → 飞书）
- 飞书上的更改也要反映回本地（飞书 → 本地）

**背景**：之前飞书同步效果不佳，改为"本地优先"思路。先跑通本地，二期再做双向同步。

**注意**：系统字段中 record_id / synced / local_updated_at 原为飞书同步设计，一期保留不用，二期启用。

### 设计原则

- 采集表的「已同步」字段已更名为「已解析」，仅作为步骤2解析成功的动作反馈
- 不额外加「是否已入账号表」字段，靠 sec_user_id 关联 + 账号表「已获取信息」反查
- 步骤4天然承担步骤3失败记录的重试通道
