# DoukHub 更新日志

## 2026-09-21 v2.3.6 修复：`.form-group label` 抢样式，导致弹窗内多处控件文字贴顶 / 选中态失真

### 1. 🔴 根因：容器级规则压过组件类

`style.css` 里有一条跨页生效的规则：

```css
.form-group label, .field-label {
  display: block; margin-bottom: 6px;
  font-weight: 600; font-size: 12px; color: var(--dh-text-secondary);
}                                    /* 特异性 11 (0,1,1) */
```

它命中 `.form-group` 内的**每一个 `<label>`** —— 包括那些本身就是"组件"的 label。
这些组件类只写单类选择器（特异性 10），于是被逐条压掉：

| 属性 | 被压成 | 后果 |
|---|---|---|
| `display: inline-flex` | `block` | ⚠️ **`align-items:center` 失效 → 文字贴到顶部**（本次用户看到的正是这个） |
| `font-size: 13px` | `12px` | 同排控件字号忽大忽小 |
| `font-weight: 500` | `600` | **未选中项也是粗体**，选中/未选中几乎分不出 |
| — | 多出 `margin-bottom: 6px` | flex 行按外边距盒居中，组件整体**比同行文字高 3px** |

实测偏差：`.sched-mode` 文字垂直偏差 **−7px**、`.sched-day` **−6.4px**、
`.switch-item` 相对同行文字 **−2.72px**。

### 2. 修复内容（3 个组件类 + 3 处媒体查询）

- `schedule.html` `.sched-day`（星期多选）：加 `.form-group` 前缀 + `margin-bottom:0`、
  `font-weight:500`、`line-height:1`、`display/align/justify` 显式声明；高度 **32px → 34px**
  （与 §13 控件基线一致，之前漏了它）。
- `schedule.html` `.sched-mode`（每天多次的"指定时间点 / 固定间隔"）：同上，补 `justify-content:center`。
- `settings.html` `.switch-item`（经典开关）：加前缀 + `margin-bottom:0`。
- ⚠️ **连带修复**：桌面规则特异性升到 20 后，媒体查询里原来那条单类规则（10）会被反过来压住，
  窄屏触控放大全部失效 → 媒体查询内同步补 `.form-group` 前缀。

### 3. 验收（Playwright 实测）

- 三个组件文字垂直偏差：**−7 / −6.4 / −2.72px → 0 / 0 / +0.28px**
- `.sched-day` 七天垂直中心离散 **0.0px**；未选中字重回到 **500**、选中态 **600 + accent**
- 明暗双模式一致；1440 / 768 / 375 三档视口无横向溢出、零 JS 与控制台错误
- 移动端仍正确放大：星期按钮 36px、方式 chip 40px、时间输入 40px
- 全项目扫描：其余页面（`/table`、`/dedup`、`/collect/detail`）无同类组件，未受影响

### 4. 顺带

`docs/UI-DESIGN-SYSTEM.md` §18 从"`.form-group small` 单坑"扩写为"**容器级规则压过组件类**"这一类坑，
新增 §18.4（`<label>` 组件变体 + 媒体查询连带坑 + 新增组件自检两问）。

## 2026-09-21 v2.3.5 定时任务：修「方案已删除」误报 + 移除静态时区胶囊

### 1. 🔴 修「方案已删除」误报（preset_id 类型漂移）

任务卡片一直把好好的方案标成红色「方案已删除」。根因是**老库列类型漂移**：

`scheduled_tasks.preset_id` 实测列类型是 **TEXT**，而建表 DDL 写的是 **INTEGER** ——
旧表列名「等级筛选」本就是文本，迁移只做 `ALTER TABLE RENAME COLUMN`，**列类型不跟着改**。
于是同一个字段：写入是数字 `1`，读出来是字符串 `"1"`；而方案 id 是数字 `1`
→ **前端 `===` 严格比较必然失配**。（新装库是 INTEGER，所以此问题**只在迁移过的老库上出现**。）

影响三处，同一根因：

| 位置 | 症状 |
|---|---|
| `schedule.html` 列表查找 | 误报「方案已删除」。后端 `get_preset()` 用的是 `==`，**任务其实照常能跑** |
| `schedule.html` `fillPresetOptions` | **编辑任务时方案下拉回填成第一个方案** —— 打开编辑框直接保存会**静默换掉任务的采集方案** |
| `main.py` `preset_map` | API 的 `preset_name` 恒为空串（`hash("1") != hash(1)`，dict 查不到） |

修法（治本 + 兜底）：

1. `database.py` 新增 `_normalize_scheduled_task()`，在 `get_scheduled_task(s)` 出口把 `preset_id`
   统一转成 int —— **一处治本**，上层只面对一种类型；
2. 前端 2 处比较改为 `String(p.id) === String(t.preset_id)` 兜底；
3. 后端 `preset_map` 改用字符串键。

**刻意不做**表重建改列类型（SQLite 需整表重建，风险高收益低）。

验收：`preset_id` 现在返回数字 `1`、`preset_name` 返回「日常 4星+（增量）」；
前端 13 项检查全 PASS（回填 `'2'`/`'3'`/`2` 均正确，明暗 × 三档视口无溢出、零 JS 错误）。

### 2. 移除「调度概览」里的静态时区胶囊

那颗「时区 Asia/Shanghai」是 `scheduler.py` 里**硬编码、用户改不了**的常量 —— 值恒定不变，
不承载任何状态信息；却借用了 `workflow-status` **状态徽章**的样式，会让页面上"徽章"这个视觉符号
失去信号价值。时区信息在新建/编辑弹窗里已按需提示（「按本地时区（Asia/Shanghai）」），
那才是它有用的地方。故移除，空的 `workflow-actions` 容器一并清掉（无 JS 引用）。

---

## 2026-09-20 v2.3.4 弹窗关闭按钮统一 34px

### 1. ✕ 按钮 30px → 34px（全站）

`.dh-modal-close` 定义在 `base.html`，全站 **9 处弹窗**共用（确认框 / 后台任务面板 / 磁盘面板 /
定时任务新建与历史 / 采集方案管理 / 批次详情 / 备份清理 / 目录选择器）。原来是 30px，
比弹窗里的输入框、按钮（34px）矮一截。统一到 34px 后与全站控件基线一致。

验收：6 个可达弹窗实测 **34×34、图标垂直居中偏差 0**，三档视口（1440/768/375）无溢出。
定时任务弹窗 10 个控件回归全绿。

---

## 2026-09-20 v2.3.3 定时任务弹窗：控件高度统一

> 主题：弹窗里的控件高低不齐（30 / 34 / 35 / 35.6px 五种值），横排放一起就显歪。

### 1. 全弹窗控件统一 34px

实测确认：**每行的垂直中心是齐的（离散 0.0px）**，问题不在位置而在**高度参差**：

| 控件 | 原高 | 修后 | 原因 |
|---|---|---|---|
| 右上角 ✕ | 30px | 34px | base.html 全局值，本页覆盖 |
| 快捷模板 ×4 | 30px | 34px | 本页值 |
| 任务名称 / 底部按钮 | 34px | 34px | 基准 |
| 采集方案 / 执行频率 `<select>` | **35px** | 34px | 全局只给了 `min-height:34px`，**原生下拉框会撑高** |
| 到点 `<input type=time>` | **35.6px** | 34px | 同上 |
| 「每天多次」方式 chip | 36px | 34px | 上一轮遗留 |

### 2. 快捷模板按钮的居中方式换掉

原来用 `display:block` + `text-align:center` —— 那只管水平方向，
垂直方向靠 `line-height` 撑，**行高算法随字体变，不是可靠的居中**。
改为 `inline-flex + align-items/justify-content:center`，与弹窗内其他按钮同一套机制。

### 3. ⚠️ 顺手排掉两个 CSS 陷阱（都只在窄屏暴露）

- **`min-height` 抬不动已定死的 `height`**：桌面把控件 `height` 钉成 34px 后，
  移动端媒体查询里只写 `min-height:40px` **完全无效**，375px 实测仍然是 34px。
  移动端必须显式再写一遍 `height:40px`。
- **媒体查询的覆盖要连"属性选择器数量"一起算**：桌面基线
  `#modal .box input[type="time"]`（特异性 2,3,1）**强于**移动端的 `#modal .box input`（2,1,1），
  导致窄屏那条 40px 规则被反压。移动端规则把具体分支抄全才生效。

两个坑的共同后果都是"**窄屏触控目标缩水到桌面尺寸**"，且**宽屏验证时看不出来**。

---

## 2026-09-20 v2.3.2 定时任务：一天多次采集

> 主题：原来只有「每天 / 每周 / 每月」，**一天要跑几次填不出来**。
> 新增「每天多次」档，两种填法覆盖绝大多数场景，并给高频轮询上了硬闸。

### 1. 新增「每天多次」频率档

下拉里在「每天」之后插入「每天多次」，内部给两种填法（单选切换）：

| 方式 | 适合 | 生成示例 |
|---|---|---|
| **指定时间点** | 想固定在几个整点/半点跑 | `0 2,8,14,20 * * *` |
| **固定间隔** | 想均匀铺开（每 N 小时） | `0 0,6,12,18 * * *` |

- **指定时间点**：默认给 4 个（02:00 / 08:00 / 14:00 / 20:00），可增删，**上限 12 个**。
  新增的默认值是「上一个 +6 小时」，避免连点几次都在同一时刻。只剩 1 个时删除按钮禁用。
- **固定间隔**：选 1/2/3/4/6/8/12 小时 + 一个起始时间。
  起始时间不是摆设——「从 8 点开始每 6 小时」= 8/14/20/次日 2 点，会**整体平移到最近的整点上**
  （`0 2,8,14,20 * * *`），不是简单地 8 点起加 6。
- 下方实时显示这家话讲的结果（「0:00、6:00、12:00、18:00 各执行一次」），
  存之前就能确认自己排的是什么。

### 2. ⚠️ 间隔下限：1 小时硬拦，2 小时提醒

定时采集本质是**周期性请求平台接口**，跑太密会直接触发风控把账号打废。所以：

- **< 60 分钟：直接拒绝保存**（前后端双拦，各给一句说明）。
  前端数一遍相邻时间点的最小间隔（**含跨零点回绕**，23:30 与 00:15 算 45 分钟）；后端独立复算一遍，
  防止有人绕过界面直接打 API。
- **60~120 分钟：只提醒不拦**（`is-warn` 琥珀色提示，不是错误色）。
  用户明确知道自己在做什么时候不该被拦。
- 后端实现读的是 APScheduler 编译后的 `trigger.fields` 表达式（`AllExpression` / `RangeExpression`
  自己展开成一天内的分钟偏移取最小差），**不靠正则猜表达式**；
  「日」或「周」字段有限制（每周一、每月 1 号）时一天最多跑一次，直接跳过判断。

### 3. 展示与回填

- 列表页中文描述从「每天执行」升级为**说清几次**：
  `每天 4 次：02:00、08:00、14:00、20:00`；展开不出具体时刻时兜底「每天多次执行」。
- 编辑已有任务时能把多时段表达式**还原回填**成时间点 chip（`0 2,8,14,20 * * *` → 4 个时间输入），
  改完再存不会把原计划冲掉。
- 每 1 小时的写法从 `0 0-23 * * *` 优化为 `0 * * * *`（等价但可读）。

### 4. 🔴 顺手修掉两个真 bug

- **「新建定时任务」弹窗打不开**：`fillPresetOptions()` 调用的 `escapeHtml()`
  **本模板里从未定义**（只存在于 collect / settings / sync-import 三个页面各自的作用域），
  一点「新建」就 `ReferenceError`，方案下拉填不上、弹窗直接不弹。
  这是**页面重构时留下的**，本模板补上本地定义（转义 `& < > " '`）。
- **表单提示文字对比度不达标（2.66:1）**：`style.css` 里有一条
  `.form-group small, .field-hint { color: var(--md-outline) }`（特异性 21），
  而 `--md-outline`(#A39E96) 在白底上只有 **2.66:1**，远低于 WCAG AA 的 4.5:1；
  本页的 `.sched-hint` 特异性只有 10，被那条规则压住。
  改为 `.sched-hint, .form-group .sched-hint, .form-group small.sched-hint` 把特异性提到 21 并靠后声明，
  **实测 2.66 → 7.25（浅色）/ 10.33（深色）**。

---

## 2026-09-20 v2.3.1 定时任务增强：失败通知 / 一次性执行 / 任务堆叠保护

> 主题：让定时任务从「设好就忘」变成「出了问题你能知道」。
> 并顺手修掉一个潜伏的表结构脱节隐患（一旦表里有数据就会整页报错）。

### 1. ⚠️ 修复 `scheduled_tasks` 表结构脱节（潜伏隐患）

线上库该表的真实列名是**中文**的（`ID / 任务名称 / Cron表达式 / 等级筛选 / 启用 / 上次运行 / 下次运行 / created_at / 同步时间`），
而代码按 `id/name/preset_id/cron_expression/...` 读写，**完全对不上**；`preset_id`、`last_run_status` 等列根本不存在。

之所以一直没暴露：该表当时是 **0 行**，空表查不出「列不存在」。
**一旦表里有数据，定时任务页就会整页崩。**

修法（无痛窗口内补迁移，不手工改库）：

- `_migrate_legacy_columns` 的 `rename_map` 补 8 条中→英映射（`等级筛选` → `preset_id`，语义已被采集方案取代）。
- `add_columns` 补 `sync_before` / `last_run_status` / `last_batch_ids`。
- 建表 DDL 列顺序对齐旧库（`ALTER TABLE ... RENAME COLUMN` 不改变列物理位置，
  顺序对齐可避免新旧库 `SELECT *` 的键序漂移）。

已用「旧结构 + 有数据」的仿真库验证：12 列全部就位，任务名 / cron / 启用 / 上次运行时间一条不丢。

### 2. 定时任务失败通知

此前凌晨任务跑挂了**看不到任何提示**，直到某天发现数据没更新。现在：

- 触发失败时写一条通知（复用 `sync_history`，`task_type='scheduled_task'`，**不新增表**）。
- 侧栏「后台任务」按钮转**警示琥珀色**（用 warning 而非 danger：失败已发生但不会继续恶化，
  与「磁盘要满了」的持续风险分级）；并有未读小圆点。
- 后台任务面板新增 **「失败」标签页**，列出任务名 / 时间 / 具体错误，可一键「清空记录」。
- 页面打开时自动轮询（60s 一次，与磁盘同频；失败是低频事件，不跟 2.5s 任务轮询一起刷）。

⚠️ 「清空记录」用**已读水位线**语义，不是永久静音的哨兵：
只盖住「点清空那一刻已存在的记录」，之后新产生的失败照常提醒。
（早期的 `-1` 哨兵写法会把此后所有新失败一并永久隐藏 —— 用户再也不知道任务挂过。）

### 3. 一次性执行（指定日期时间）

- 新增 `@once:YYYY-MM-DD HH:MM` 表达式，**复用 `cron_expression` 字段**，零表结构改动；
  所有既有增删改查 / 校验 / 展示路径原样可用，只在调度与展示两处特判。
- 调度层 `build_trigger()` 按表达式类型分流：一次性走 `DateTrigger`，其余走 `CronTrigger`。
- 一次性任务**跑完自动停用**并撤掉 job，不在列表里留一个永远不再触发的任务让用户困惑。
- 创建时校验「时间必须晚于当前时间」，过期直接拒（400）。
- 卡片上有「仅一次」标签（用 accent-soft 而非语义色 —— 它是类型标识，不是状态告警）。

### 4. 快捷频率模板

弹窗里加三个一键填好的常用计划：`每天凌晨 2 点` / `每周一早上 8 点` / `每月 1 号凌晨 3 点`，
省掉选频率 + 调时间 + 点星期三步骤。选「仅一次」时自动隐藏（此时模板无意义）。

### 5. 任务堆叠保护

上一次触发还停在 `running`（采集没结束）时，本次**直接跳过**，不排队、不重试：

- 理由：定时任务的价值在「按点跑一次」，迟到的第 N 次没有意义，
  排队反而会和正常调度叠在一起把设备压满。
- 手动「立即执行」是有意触发，**不受此限制**。
- 新增 `skipped_busy` 状态（第五个 `last_run_status` 值），前端映射 warning 色 + `skip-forward` 图标，
  悬停有说明「上一次采集还没结束，本次按计划跳过」。
- ⚠️ 带 12 小时**僵尸自愈**宽限：超过 12h 仍停在 running 视为进程崩溃残留，放行下一次触发。
  否则该任务会被永久锁死，再也跑不起来。

### 6. 定时任务模块归位与页面重构（承接上一轮）

- 侧栏位置移到「采集」组第三子项（单作品采集 → 增量采集 → 定时任务）。
- 执行历史改为按 `collection_batches.scheduled_task_id` **精确匹配**归属，
  不再用「任务名 == 方案名」的旧口径（老批次该列为 NULL 时才回退）。
- 「立即执行」：后台跑、立刻返回；**已暂停的任务也能手动跑一次**，
  不改启用状态与 cron，前端靠 5s 轮询跟进状态。

---

## 2026-09-19 v2.3.0 抖音接口直连化 + 采集 Fail Fast

> 主题：把「什么都丢给 TTD 内核」改成**能直连就直连、直连失败才回退 TTD**。
> 解析与资料获取从 ~6s 降到 ~0.4~1s，并顺带解决新版抖音风控导致的采集失败。

### 1. 短链接解析：直连重定向替代 TTD

`v.douyin.com` 分享码会 302 跳到完整用户页 URL。新增 `_direct_redirect()` 直连跟随重定向
（**~0.4s**），失败才回退 TTD `/share`（~6s，含内置 wait）。失效链接仍会被正确判定：

- 跳到作品/直播页 → `not_profile`（本身是作品链接，不算失效）
- 跳到平台首页 → `bad_link`（短码已失效），**不再多发一次 TTD 请求**

### 2. 账号资料获取：抖音 API 直连（ABogus 签名）

新增 `fetch_user_profile_direct()`，直连 `aweme/v1/web/user/profile/other/`（**~1s**），
回退 TTD `/douyin/account`。账号资料优先直连，使得「同步账号 / 刷新账号」不再受 TTD 波动影响。

直连技术要点（本轮踩坑沉淀）：

| 问题 | 现象 | 解法 |
|---|---|---|
| TLS 指纹 | httpx 请求被 **403** | 改用 `curl_cffi` 模拟 Chrome（`impersonate="chrome146"`） |
| Argus 风控 | `403 Blocked by ArgusSecurityPlugin Uifid Not Found` | 从 Cookie 提取 `uifid`，**URL 参数 + Header 都要带**，并加 `x-tt-argus: 1` |
| ABogus 签名 | 新版构造函数签名变化 | `ABogus(user_agent)`，`get_value(params_str)` 单参调用 |
| 接口版本 | 参数过旧 | `version_code/version_name` 跟随抖音升到 290100 / 29.1.0 |

顺带修正：TTD `/douyin/account` 返回的是**作品列表**（账号资料藏在 `data[0].author`），
原先按单个对象解析会拿到空资料 —— 现在兼容 list / dict 两种形态。

### 3. Cookie 验证：直连优先 + 失败原因可见

- `validate_cookie()` 抖音优先走直连 API 验证（快且不依赖 TTD Server），TikTok 回退 TTD。
- 返回值由 `bool` 改为 **dict**（`{status, message, nickname}`），调用方同步适配。
- `cookie_cache` 表新增 **`验证说明`** 列：记录失效原因（如「登录态已过期」），
  在表浏览里以红色徽章展示。**该列不覆盖用户手填的「备注」**，验证通过时自动清空。
- 统一新增 `get_cookie_list()`：库中启用的 Cookie 为空时，**回退读取 TTD `Volume/settings.json`
  的 `cookie` / `cookie_tiktok`**（此前 8 处各自读库的逻辑已收敛到这一处）。
- 单作品下载的 Cookie 选择改为 **LRU 轮换**（最久未使用优先），并回写使用记录。

### 4. 增量采集：Fail Fast 自动止损

此前 Cookie 失效后，批次会**把剩下所有账号逐个跑完才报错**，白等几十轮。现在：

- 连续 **5 个**账号返回登录态类错误 → **立即终止 TTD 进程**中止批次，并把该 Cookie 标记为失效。
- 若还有**其他可用 Cookie** → 自动换 Cookie 重跑失败/未完成的账号（只自动换一次），
  不用人工重新建批次。
- 计数只针对**登录态类错误**，API 偶发风控的临时失败不会误触发；
  且计数发生在账号匹配**之前**，避免 sec_user_id 不匹配时漏计。
- 批次终止原因写入 `collection_batches.message`，在批次列表可见。

### 5. TTD 内核：适配 5.8 风控 + 一键更新

- TTD 5.8 的 `im/user/info` 接口被风控（**Cookie 有效也返回空**），导致批量采集匹配不到作品而中断。
  现已 patch `Extractor.__select_item`：匹配 `sec_uid` 失败时**返回第一个作品继续**而非抛错
  （应对共创作品场景）。
- `source=True` 取原始 API 数据，避开新版 TTD `source=False` 对 Volume 目录配置的依赖（会 500）。
- 采集节流：每个账号处理完都暂停（不管成功失败），降低风控概率。
- 新增 **内核一键更新**：`POST /api/kernels/{name}/update`（状态页与内核卡片均有按钮），
  自动 stash → pull → stash pop，冲突时丢弃本地修改；同时显示内核版本号。
  ⚠️ 注意：此处的 stash 作用于 **TTD 内核仓库**，与 DoukHub 主仓库无关。
- 所有内核 subprocess 调用加 `CREATE_NO_WINDOW`，**不再弹 CMD 黑框**。

### 6. 其他

- 表浏览：`验证说明` 列加入只读字段、列宽、权限映射与专用渲染器。
- 移除采集流程中多余的固定 `sleep`（0.3/0.5s），改由 TTD 侧节流统一控制。

### 升级提示

- `cookie_cache` 与 `collection_batches` 表会自动补列（`验证说明` / `message`），无需手动迁移。
- 直连 API 依赖新增的 `curl_cffi`，请确保依赖已安装（`pip install -r requirements.txt`）。

## 2026-09-17 v2.2.12 移动端适配修复（375px 实测）

> 方法：此前所有 UI 审计都在 1440px 宽做，本轮首次把视口压到 375 / 768px，
> 用 Playwright 量真实几何（`getBoundingClientRect` + `Range.getClientRects`
> 量文字真实左边界），而不是只看"有没有横向滚动"。
> **全部修正都限定在 `@media (max-width: 768px)` 内，桌面样式一字未改。**

### 1. 全站：页面标题被汉堡按钮遮挡（最严重）

汉堡是 `position: fixed` 的 (12,12,44×44)，而 `.main` 的首行内容**也从 (12,12) 开始** ——
两者完全重叠。实测 `h2` 文字左边界 = 汉堡右边界 **-44px**，即标题**完全压在按钮底下**，
每个页面的标题首字都被盖住；副标题更长时会被挤成断词换行（如 dedup「…录里的重复文件」、
status「TTD、XHS 与后台任务健康状 / 态」）。

修法：给两种页头写法（`.page-header > h2` / `.page-header > .page-title-wrap` /
`.page-head > div:first-child`）在窄屏加 `padding-left: 56px` + `min-height: 44px`
并与汉堡垂直居中。**只缩标题容器、不缩整块 header**，操作按钮行仍是通栏。
复测 11/11 页面文字左边界统一为 68px，与汉堡间距 12px。

### 2. collect：批次卡片被桌面列宽撑爆（533px vs 视口 375）

移动端卡片是塞在 `<tr><td colspan="11">` 里渲染的，**表格宽度被桌面 11 列的表头撑到 533px**，
于是卡片右侧被裁掉 158px、表头文字互相挤压重叠。
修法：`#batch-table` 改 `table-layout: fixed` + 窄屏隐藏表头（卡片自带字段标签），
grid 改 `minmax(0, 1fr)`（默认 `1fr` 的 min-content 下限会把列撑爆）。
实测卡片宽 **533 → 313px**，字段值正常右对齐换行。

### 3. 移动端小字号下探到不可读

实测 375px 下仍有 9~11px 的文字承担正文职责，逐处抬到可读下限（桌面值不变）：

| 位置 | 改前 | 改后 |
|---|---|---|
| collect 统计条标签 `.preview-stat-label` | **9px**（≤420px） | 11px |
| collect 运行状态卡标签 `.stat-label` | 10px | 11px |
| table 状态徽章 `.tbl-tag` | 10.5px | 11.5px |
| table 计数徽章 `.tbl-count-badge` | 11px | 12px |
| dedup 主/次徽章 `.scan-badge` | 10.5px | 11.5px |
| collect/overview 批次表 `th/td` | 11px | 12px |
| collect_detail 下载方式按钮 / chip | 11px | 12px |

### 4. 移动端触控目标与页头布局

- 表页 tab 按钮 **29px → 38px** 高（低于 36px 触控舒适线）；`.icon-btn` 提到 36×36；
  dedup「全不选」提到 32px；卡片内的搜索/筛选输入框提到 36px（排除 AG Grid 内联编辑器）。
- `.page-head` 桌面是「标题 | 操作」左右并排，375px 下会挤成断词 —— 补上 `.page-header`
  已有的堆叠处理，主操作（`btn-primary`）在窄屏独占一行撑满，主次更清楚。
- 表页底部加载状态条改 `flex-wrap: wrap`，不再三块并排挤成两行小字。

### 验证

- 桌面回归：16/16 路由 **控制台错误 0、4xx 0、横向溢出 0**
- 窄屏 375 / 768 / 1440 × 16 路由：**横向溢出 0 / 0 / 0**
- 标题让位：11/11 页面文字左边界 68px（改前大量为 12px，即被遮挡）
- 残留 <12px 元素：31 处，全部是徽章/标签类（非正文数据），已从 9~10.5px 抬到 11~11.5px

## 2026-09-17 v2.2.11 统计条颜色语义修正 + 移动端抽屉补 ESC

> 方法：用 Playwright 同时量出「计算色 + 有效背景色」再算 WCAG 对比度，
> 并实测多浮层叠加时的 ESC 行为（抽屉 + 目录选择器同时打开的时序）。

### 1. 统计条：分类计数不再借用语义色

collect 页「可采 / 首次 / 增量 / 跳过」原本是 **蓝 / 橙 / 绿 / 灰** 四色，但这四项是**分类计数**，
不是状态。橙=警告、绿=成功是全站统一语义，读起来容易把"首次"当"警告"、把"增量"当"成功"。

修正为：**主指标（可采）蓝 + 分类计数正文色 + 跳过次级灰**；运行统计条里的
成功/失败仍保留绿/红（那才是真状态）。

### 2. 顺手修掉一个 WCAG 不达标（本轮量出来的）

统计条数字是 20px/700，按 WCAG 属「大字号」，门槛 3:1。而「跳过」此前用
`--dh-text-muted`（浅色 `#A39E96`）—— 它在浅色底上仅 **2.38:1**，连大字号线都不到。

该令牌在设计文档里明确只服务时间戳/占位这类 ≤12px 辅助小字，**用在大数字上属越权**。
统一改为 `--dh-text-secondary`：浅色 **2.38 → 6.49**，深色 **9.38**。

### 3. 移动端抽屉导航补 ESC 关闭

全站其它浮层（确认框 / 任务面板 / 目录选择器 / 批量详情）都支持 ESC，只有 ≤768px 的
侧边抽屉不支持 —— 拖开菜单后按 ESC 没反应，属交互一致性缺口。

实现要点：键处理注册在**捕获阶段**。项目里浮层的 ESC 处理器都在冒泡阶段、注册顺序早于本处，
若同样用冒泡就会"一次 ESC 连弹窗带抽屉一起关"。捕获阶段能在其它处理器改动 DOM 之前读到状态，
所以先判 `document.querySelector('.dh-show')`：有更上层浮层就放行，让它自己处理。

### 验证

- 抽屉：汉堡打开 → ESC 关闭 ✓；**抽屉 + 目录选择器同时打开 → 第 1 次 ESC 只关弹窗（抽屉保持）、
  第 2 次 ESC 才关抽屉** ✓；1440px 桌面端不受影响 ✓
- 统计条：两条统计条共 10 个数字 × 明暗双主题 **全部达标**（最低 4.49:1）
- 全站 16 路由回归：0 问题；控制台错误 0

---

## 2026-09-17 v2.2.10 修复「保存设置」抹掉同键未提交字段 + 补 favicon

> 方法：脚本对比设置表单 `name` 清单 vs 配置实际键，找出会被整体替换抹掉的字段；
> 再用 Playwright 真实打开设置页点「保存设置」端到端复验。

### 问题：顶层键整体替换，抹掉表单没提供的字段

`POST /api/settings` 原实现是整键替换：

```python
for key, value in data.items():
    config.set(key, value)      # local / feishu 这种整个字典被替换
```

而设置表单只提供每个顶层键下的**部分**字段。核对结果：

- `api`：配置有 3 键（`enabled` / `api_key` / `default_resolve_mode`），表单只给 `enabled`
  → **每次保存设置都会抹掉 `api_key` 与 `default_resolve_mode`**
- `cookie` / `downloader` / `feishu` 当前恰好字段数对得上（2/2、6/6、6/6），属**脆弱的巧合** ——
  今后任一键新增配置项而漏加进表单，保存一次即丢

影响面：`api_key` 历史值一直是空（5 份备份均为空）、`default_resolve_mode` 恰好等于默认值，
**本次未造成实际数据损失**；但一旦开启 API 并生成 Key，此后每次保存设置都会把 Key 抹掉，`/api/v1/*` 随即失效。

同时**排除**了本机制与 09-17 中午「存储方案消失」的关系 —— 表单根本不触及
`single_work` / `storage_profiles`（核对脚本已列出未触及的顶层键清单）。

### 修复

- `POST /api/settings` 改为**补丁深合并**：新增 `_merge_settings_patch(base, patch)`，
  仅两侧同为 dict 时递归，其余类型整体覆盖。表单未提交的字段从此保留；
  正常改值、以及**刻意清空某字段**（提交空串覆盖标量）均不受影响。
- 只动后端一处，前端无需改 —— 表单值全部由 Jinja 服务端渲染（`value="{{ config.x }}"`），
  不存在「异步加载中表单为空 → 提交空值」的窗口，故原计划的「加载完成前禁用保存」未实施。

### 验证（端到端）

生成 API Key → 真实打开设置页点「保存设置」→ 回读：

- `api_key` 保留 ✓（修复前必丢）
- `default_resolve_mode` 保留 ✓
- 普通字段仍可正常保存：`concurrent_accounts` 3 → 7 → 还原 3 ✓
- 清空标量仍有效：`api_key` 置空成功 ✓

### 顺带

- 补 `<link rel="icon">`：此前只声明了 `apple-touch-icon`，浏览器退而请求 `/favicon.ico` → 404，
  是设置页控制台唯一一条错误。复用已有 `app/static/images/icon-192.png`。

## 2026-09-17 v2.2.9 表单控件统一与目录设置框重做

> 方法：Playwright 实测全站 16 个路由的**全部可见表单控件（45 个）**，按 computed style 分组比对，
> 再对走浏览器默认样式的控件做修复前后实测。

### 根因：`.input` 被使用，但从未定义

全站 4 处 `class="input"`（设置页「新应用数据目录 / 新备份目录」+ 目录选择弹窗两处）在 CSS 中
**没有任何定义**，一直掉到浏览器默认样式：`2px rgb(118,118,118)` 边框 + 直角 + 仅 **19px 高** ——
高度只有旁边控件（34px）的一半。这是"控件风格不统一"最刺眼的来源。

### 控件统一

- 建立统一基线：`.form-group input/select/textarea` 与 `.input` 合并为同一组规则 ——
  **34px 高 / 6px 圆角 / 1px `--dh-border` / 13px 字号**，与 `.btn`(34px) 严格等高，输入框与按钮并排不再错位；
  hover 由近黑的 `--md-on-surface` 收敛到 `--dh-border-strong`；焦点环 2px → 3px。
- 补齐 5 处裸奔控件：设置页 2 处、目录弹窗 2 处、dedup 回收区目录（补 `input` 类）。
  复检：**走浏览器默认样式的可见控件 5/45 → 0/45**。
- 勾选框统一着色：`:where(input[type="checkbox"], input[type="radio"]) { accent-color: var(--dh-accent) }`
  （`:where()` 零特异性，不覆盖 AG Grid 自带主题）。

### 目录设置框重做（设置页 / 备份页 / 目录选择弹窗）

- 新增 `.path-pill` **只读路径胶囊**：柔和底 + 强调色图标 + 等宽路径（超长省略、`title` 悬停看全）+ 柔和图标按钮，
  替代原「灰底贴边 mono 文字 + 孤立方块按钮」。
- 新增 `.field-affix`：目标目录输入框与「选择文件夹」按钮**合成一个控件**（按钮内嵌在输入框右端），
  替代旁挂竖排三按钮，右侧不再参差。
- 新增 `.field-actions`：操作按钮右对齐横排；同卡片内的次级分组用**虚线**分隔（比实线轻）。
- 目录选择弹窗：工具栏图标按钮统一 34px；列表由「表格行 + 分隔线」改为「圆角行 + 浅底 hover / 选中」，
  选中项用 `--dh-accent-soft` 底 + 强调色文字；空态补图标。

### 顺手修复

- 设置页复制按钮原把路径直接拼进 `onclick` 字符串 —— Windows 反斜杠会被 JS 当转义符吃掉
  （`C:\Users\Gm` → `C:UsersGm`）→ 改走 `data-path` 属性。
- 清理 backup.html 三个已废弃 class（`.backup-dir-row` / `.backup-dir-path` / `.backup-dir-note`）。

### 遗留（本次未处理）

- **select 下拉箭头仍为浏览器原生**：统一改需 `appearance:none` + 自定义箭头，但各页 select
  已有各自的 `background` 简写声明（含 ID 选择器），改动会与之冲突，收益不抵风险。
- **项目同时加载 lucide 与 Phosphor 两套图标库**：9 个模板共 117 处仍用 `ph ph-*`
  （table.html 42、backup.html 16、database.html 15、sync/cloud.html 14、dedup.html 13 等），
  两套线宽与比例有细微差异，属风格不统一的深层来源。

## 2026-09-17 v2.2.8 全站界面审计与深色模式缺陷修复

> 方法：内置浏览器（Playwright + Chrome headless）实测全部 16 个路由 × 明暗双主题共 32 次，
> 自动校验强调色数量 / 文字对比度 / 控制台错误 / 横向溢出。以下缺陷浅色下不可见，均由实测暴露。

### 深色模式缺陷

- **表单控件文字色不继承**：`button/input/select/textarea` 未显式设色时使用浏览器系统色（ButtonText，恒为黑）。
  深色下同步各页的历史时间戳、弹窗"关闭"按钮残留黑字，对比度 **1.24:1 近乎不可见**。
  → 全局补 `color: inherit`，自动跟随主题（显式设色的 `.btn-primary` 等优先级更高，不受影响）。
- **导航激活态不跟随主题**：`--accent-active` 硬编码 `#0061A4` 且深色未覆盖，激活项始终是暗沉深蓝；
  `--text-on-accent` 恒为白，压在深色亮蓝底上仅 2.2:1。
  → 改为引用 `--dh-accent` / `--md-on-primary`，深浅各 6.3:1 / 8:1。
- **账号作品弹窗深色割裂**：`account-works.js` 注入的约 40 处硬编码浅色（白底 + 暖黑字），深色下是白底黑字弹窗 → 全量令牌化。

### 令牌一致性

- 清理全项目 **156 处** `var(--token, 回退值)`：回退值已与真实令牌漂移（如 `--dh-border` 回退写成 `#eee9e2`、实际 `#ECE7DF`），
  令牌唯一来源回归 `style.css`；保留 JS 运行时注入的 `--sc-color` 回退。
- 紫色统一走全局 `--dh-purple`（database 页原 `#8B5CF6` 与 settings 页 `#A855F7` 不一致）。
- `--dh-success` 由 `#16A34A`(3.16:1) 调深至 `#15803D`(4.63:1)，绿色文字达到 WCAG AA。
- 补齐模板已在用、但全局未定义的 `.text-muted` 工具类。

### 视觉层级与可读性

- **collect 批次历史**：每行"重试"由实心蓝降级为描边 —— 原一屏 18 个实心蓝，违反"一屏实心强调 ≤1"。
- **table 工具栏**："+ 新增"由语义绿改为主色蓝（绿=成功，不宜作主操作）。
- **backup 指标数字**：20px 大数字原继承弱化灰(2.38:1) → 改正文色(15:1)。

### 复测结果

实心强调超标页 0 个 · 深色黑字 0 处 · 控制台错误 0 条 · 深色主题全页生效。

## 2026-09-05 v2.2.7 全局界面统一与令牌化收尾

### 令牌化残留清理（明暗切换漏网之鱼）

- 任务面板/历史日志的深色内联块（原 #0f172a 强制深色、亮色主题下违和）统一为 `.log-panel` 组件，`--dh-*` 令牌明暗自适应。
- 任务面板与历史列表的状态色表（pending/running/done/failed 等）由 JS 原色 hex 改为语义令牌（--dh-warning/accent/success/danger）。
- 设置页 scope-tag 绿/紫原色、开关轨道与滑块、激活态文字 #fff 全部换令牌；新增页局部 `--dh-purple` 令牌（明暗两档）。

### 组件统一

- 全局日志组件 `.log-panel` 与 `.workflow-log` 合并样式，任务面板、历史日志、工作流日志三处外观一致。
- 空状态组件扩展 `es-title` / `es-desc` / `es-action` 与 `compact` 变体，应用到任务面板空态、备份页"暂无备份"、同步概览"暂无账号"、数据概览加载态。
- 区块卡头图标统一 chip 化（软色圆角底 + 语义色图标），全局生效。

### 页面优化

- 数据概览：去除与页头重复的"数据概览"卡片标题，统计改为主/次方案配色的指标卡网格（悬停浮起、30px 大数字、定时任务紫色令牌化）。
- 设置页折叠卡增加展开/收起高度动画（JS 测量高度 + 过渡）。
- 全局采集进度浮条令牌化并改为胶囊样式：呼吸图标 + 细进度条，暂停态转黄色指示。

### 维护说明

- 本地 git 仓库曾因 stash 异动损坏（对象库丢失），已从 origin/main（v2.2.6）无损重建，工作区改动完好恢复。
- 已知遗留：test_workflow_ui.py 有 13 个失败为 v2.2.5 存储方案改造后的测试漂移，与本次纯界面改动无关（已用原始版本对照验证）。

## 2026-09-04 v2.2.6 表浏览筛选面板界面优化

### 筛选弹窗美化（纯界面层，数据逻辑零改动）

- 弹窗加宽 268→300px，值列表可视高度 216→264px，长标签与长值不再频繁截断。
- 新增"排序""筛选值"分组小标题，筛选值右侧回填该列去重值总数（如"12 项"），弹窗结构一眼可分。
- 弹窗头部加底部分隔线与弱色底，与正文区域分层更清晰。
- 值检索框内嵌清空按钮（有输入时出现），一键清空并重列全部值。
- 值列表行高微增更好点按，右侧记录数定宽右对齐，滚动时数字不跳动。
- 底部"应用"主按钮加宽加粗，与"清除/取消"拉开视觉层次。

### 表浏览界面优化

- 已应用筛选条件条（chip）与搜索组合框样式统一打磨，与筛选弹窗视觉一致。
- 筛选弹窗值列表加载骨架屏动画细化。

## 2026-08-31 v2.2.5 作品明细落库与采集链路优化

### 作品明细数据库化

- 新增 collection_works 表：每个作品的下载结果（标题、类型、作品地址、文件名、下载目录、状态、失败原因）在下载完成时实时写入，不再依赖事后解析日志。
- 批次详情弹窗改读数据库，打开从秒级降至毫秒级；旧批次自动回退日志解析，历史数据照常可看。
- 账号维度作品视图：表浏览账号表行内"作品"按钮与批次详情弹窗账号名点击两个入口，支持关键字搜索、状态筛选、"只看重复"开关与指标汇总。
- 作品行操作：打开文件（资源管理器定位）、打开下载目录、跳转原作品。
- 重复作品自动标记：跨批次重复提示"已存在 ×N"，同批次内重复标红"重复 ×N"，查询时实时计算，不去重写入。

### 采集链路优化

- Cookie 轮换优选：多个可用 Cookie 按"最久未用"轮换，降低风控风险。
- Cookie 失效闭环：批次内多个账号报登录态错误时，自动将本次使用的 Cookie 标记为失效。
- 失败账号自动重试：批次完成后失败账号等待 20 秒降温自动重试一轮，仍失败才最终记为失败。
- 引擎心跳看门狗：TTD 进程超过 5 分钟无输出时写入警告日志。

### 存储方案增强

- 磁盘空间检查：启动采集前检查下载目录剩余空间，不足 5GB 弹窗提醒，确认后仍可开始。
- 路径防呆：探测发现方案路径为新建目录时提示确认，避免填错路径被静默接受。
- 设置页实时探测：打开设置页自动探测所有方案路径，修改路径后自动重新探测，可用性徽标显示探测时间。
- 失败路径嫌疑提示：批次失败原因集中指向下载目录时，失败汇总给出归纳提醒。


## 2026-08-28 v2.2.4 文档整理与归档

### 维护性更新

- 文档统一归档至 docs/ 目录，新增 UI 设计系统文档（UI-DESIGN-SYSTEM.md）。
- 过期设计稿、原型 HTML、历史计划文档移入 docs/archive/ 或删除，项目根目录清理完毕。
- 启动脚本（start.bat / DoukHub.bat / tray.py）微调与路线图更新。

## 2026-08-26 v2.2.3 数据备份与文件查重

### 核心更新

- **数据备份系统**：VACUUM INTO 一致性备份、恢复前自动备份、保留最近 7 份自动清理、每日自动备份、数据库压缩（借鉴 EntHub 设计）。
- **文件查重**：源文件级去重，三级过滤（大小 → 部分哈希 → 全量哈希）快速定位重复文件；支持按哈希判定、多保留规则（路径/时间/文件名）；删除采用「移到回收区 → 用户手动删除」安全流程。
- **查重页与备份页**：新增「数据 → 数据备份」「数据 → 文件查重」两个页面，方案目录卡片式勾选、自定义回收区路径。

## 2026-08-18 v2.2.2 API 请求模式

### 核心更新

- **API 请求模式**：其他设备（手机、平板等）可通过 HTTP API 调用 DoukHub 解析或下载作品，方便局域网内设备直接使用。
- **API Key 认证**：设置页可启用/关闭 API、生成/复制/重新生成专属密钥；调用时通过 `X-API-Key` 请求头鉴权。
- **API 端点**：新增 `/api/v1/status`（服务状态）、`/api/v1/works/resolve`（解析作品）、`/api/v1/works/download`（下载作品）等端点。

### 界面与体验

- **SPA 脚本加载顺序修复**：先加载外部脚本再执行内联脚本，修复单页切换后内联脚本引用外部函数失效的问题。
- **采集详情页增强**：快速下载、单资产下载、资产多选批量下载、作品卡片信息渲染、缩略图点击灯箱查看、SSE 进度面板。
- **交互设计文档**：新增采集详情页交互设计文档与 HTML 原型（`docs/`）。
- **资产结果区优化**：资产类型彩色徽章 + 视频时长展示；下载动作收敛到底部常驻下载栏（跨卡片聚合「来自 N 个作品」+ 下载方式本地/浏览器内联，取消弹窗）；移除每卡片重复的「下载选中」按钮，全选/类型筛选仍为卡片级。

## 2026-08-17 v2.2.1 单作品采集增强

### 核心更新

- **单作品采集闭环**：支持视频、图集、实况、动图、单资产下载、文件名模板库、目录偏好、下载历史和失败重试。
- **抖音直连解析**：新增直连 API 模块（ABogus 签名），配置了 Cookie 时绕过 TTD 直接解析，耗时约 1 秒。
- **解析/下载流式进度**：SSE 流式推送每一步状态，页面实时反馈，不再静默等待。
- **下载器保活**：新增 `keep_services_alive` 开关，心跳检测 + 自动重启，TTD/XHS 进程稳定在线。
- **采集表更名分享表**：字段语义与同步流程对齐（`已同步` → `已解析`）。

### 界面与体验

- 采集详情页重构：模板拆分、交互脚本独立，页面更轻。
- 同步概览页依据本地数据推荐下一步操作，区分空来源与待补充账号。
- 明暗主题令牌与本地图标刷新机制统一，主题切换更稳定。

## 2026-08-15 v2.2.0 本地优先同步流程

### 核心更新

- **本地优先四步流程**：导入采集表、解析采集表、同步账号表、更新账号表完整跑通；本地流程不再依赖飞书配置。
- **任务队列重构**：长耗时同步改为后台串行任务，页面切换、刷新不影响执行；任务日志和统计持久化到同步历史。
- **同步页面拆分**：新增概览、导入、解析、账号、更新、云端独立页面，侧边栏导航与当前步骤状态保持一致。
- **采集表字段语义调整**：`已同步` 更名为 `已解析`，仅表示步骤2解析成功的动作反馈；同步账号表只依赖 `sec_user_id`，不再被该字段阻塞。
- **飞书二期规划落档**：飞书双向同步明确放到二期；一期先以本地数据库为主，系统同步字段暂不参与本地流程。

### 导入与解析增强

- 支持 JSON、`标签+等级@分享码`、空格分隔三种输入。
- 标签映射不区分大小写；预览中区分已映射和未映射标签。
- 平台按链接自动识别抖音 / TikTok / 小红书。
- 支持导入账号名称、粉丝数、作品数；`1.2万`、`3.5亿` 会转换为数字。
- 无数字时等级默认为 1，等级范围统一为 1-4。
- 导入统计拆分为新增、更新、恢复、重复、跳过、失败，历史记录会写入具体跳过原因和失败详情。
- 导入完整主页链接时可直接提取 `sec_user_id`，并按用户 ID 与已有短分享码记录合并，避免同一账号产生重复采集记录。
- 解析后发现不同分享码指向同一账号时，自动合并等级和标签并软删除重复记录。

### 账号表与错误处理

- 账号表按 `sec_user_id` 去重；重复账号只合并等级、标签，不重复创建。
- 软删除账号再次出现时自动恢复，不再因唯一键冲突失败。
- 新增账号表 `获取错误` 字段，失败时记录原因，成功后自动清空；可手动编辑。
- 同步账号表在缺少 TTD 或 Cookie 时不再整单失败：仍会创建/恢复账号基础数据并合并等级标签，仅跳过详情获取。
- 更新账号表按账号自身平台调用详情接口，不再写死抖音。
- 步骤4只更新账号表，不反向修改采集表。
- 数据库表统计只统计未软删除的有效记录。

### 表浏览与交互

- 表浏览字段按业务优先级排序；错误信息前置，ID 类字段后置。
- 列宽调整保存到本地，刷新后不再丢失。
- 批量编辑列出全部可编辑字段，并按字段类型自动切换文本、数字、复选框和选项输入。
- 优化移动端导航和页面切换体验，修复多次切换页面后按钮无响应、历史记录无内容等问题。
- 导入预览、执行结果、历史详情与后端解析行为保持一致。

### 服务稳定性

- TTD 启动器注入 `/douyin/user/profile` 接口，账号同步可获取昵称、粉丝数、作品数等详情。
- TTD 请求超时提升到 30 秒，超时、连接失败、空返回会写入明确错误信息。
- TTD / XHS 服务改为 HTTP 健康检查，输出写入日志文件，心跳连续失败后自动重启。
- 修复服务状态误判和管道阻塞导致的卡死问题。

### 文档与测试

- 新增 `DATABASE_GUIDE.md`，说明三张表字段、四步流程、重复处理、错误处理和编辑边界。
- 新增 `ROADMAP.md`，明确一期本地功能和二期飞书双向同步规划。
- 修正飞书同步层残留的 `已同步` 字段命名，测试套件对齐 v2 本地字段结构。
- 本版本验证：`222 passed`，应用与测试代码语法编译通过。

## 2026-07-17 云端同步 v2 重构（方案 A 稳定版 v2.1.0-sync-a）

### 同步架构重写

完整重写 `feishu_sync.py`，引入「字段级单向同步」（方案 A）。

**核心改动：**
- **字段命名规范**：业务字段中文（与飞书 100% 一致），系统字段英文（`record_id`/`is_deleted`/`synced`/`created_at`）
- **冲突解决**：按字段归属决定方向（人工字段飞书赢，API 字段本地赢），详见 [SYNC_DESIGN.md](./SYNC_DESIGN.md)
- **业务唯一键去重**：采集表=分享码，账号表=sec_user_id，Cookie表=Cookie
- **删除同步**：飞书端直接删除 + 本地端软删除墓碑，三层安全保护
- **路由简化**：从 6 个简化为 2 个（`/api/feishu/sync` 增量、`/api/feishu/sync/full` 全盘）
- **启动时自动增量同步**：FastAPI lifespan 后台触发

**修复的 bug：**
- 飞书端删除记录被本地推回去恢复（关键修复）
- 飞书端重复业务键导致 record_id 反复横跳
- 比例保护/空结果保护触发时 synced=1 孤儿被推回飞书
- `result.update()` 覆盖前面步骤的 failed/errors
- 飞书表字段「Share→分享码」「同步状态→已同步」自动重命名

**测试：** 195 个测试通过（含 60 个同步逻辑测试）

**已知限制（方案 B 将解决）：**
- 不是真正的双向同步，在错的端修改会被覆盖
- 详见 [SYNC_DESIGN.md](./SYNC_DESIGN.md)

### 字段归属规则

- ☁️ 云端赢：等级、标签、备注、启用、采集类型、账号名称等（在云端修改有效）
- 💻 本地赢：sec_user_id、粉丝数、作品数、昵称等（在本地修改有效）
- 🔒 元数据：分享码、平台、Cookie 字符串（创建后不变）

## 2026-07-11 同步流程重构 + 飞书自动集成

### 交互重构

- **步骤重命名**：三步同步按钮从"快速同步/更新采集表/同步账号表"改为更自解释的"导入账号/解析链接/获取详情"。一键同步改为"一键执行"。
- **飞书按钮精简**：删除了同步页面原有的三个飞书按钮（本地→飞书、飞书→本地、双向同步），替换为一个"双向校准"按钮，放在步骤区底部，用于手动纠偏。

### 飞书自动集成（syncer_v2.py）

- **步骤1 自动拉取**：`import_to_collection()` 结束后自动调用 `FeishuSyncer.sync_collection_from_feishu()`，把飞书采集表的新增记录拉到本地 DB，与文本导入的记录合并去重。飞书拉取失败不影响文本导入结果。
- **步骤3 自动回写**：`sync_to_account()` 结束后自动调用 `FeishuSyncer.sync_account_to_feishu()`，把获取到的账号详情（昵称、粉丝数等）推回飞书账号表。SSE 流中会实时报告"正在回写飞书..."和回写结果。回写失败不影响本地同步结果。
- **飞书同步保留手动入口**：`/api/feishu/sync/all` 端点不变，前端"双向校准"按钮调用它做全量对齐。

### 设计原则

用户只点一次"一键执行"，数据就在本地 DB 和飞书表之间自动流转完毕。手动校准按钮仅用于数据不一致时的纠偏，不是日常操作。

---

## 2026-07-11 综合审查与优化

### 代码修复

- **collector.py**：`resolve_short_url` 方法引用了未定义的 `logger`，异常路径会抛 `NameError`。改为使用模块级 `_logger`（`import logging` + `logging.getLogger("doukhub.collector")`）。该 bug 在短链接解析失败时会吞掉原始异常，导致同步步骤 2 静默失败。
- **syncer_v2.py**：`merge_tags` 方法把所有标签强制转小写，违反文档约定（"大小写不敏感，保留原样"）。改为以小写做去重 key、保留原始大小写。例如 "COS" 不再被降为 "cos"。
- **Dockerfile**：`EXPOSE 8080` 与 `main.py` 实际监听的 2999 不一致。改为 `EXPOSE 2999`；同时在 Docker 构建阶段安装 pytest 便于 CI。
- **tests/test_api.py**：`test_browse_dir_root` 在多盘符 Windows 上断言 `current == Path.home()`，但多盘符时 API 先返回驱动器列表（current 为空），导致 79 项测试中 1 项失败。放宽断言为仅检查返回结构合法。

### UI 移动端适配

- **base.html**：完整重写响应式布局。768px 以下侧边栏变为抽屉式（汉堡菜单 + 遮罩），统计卡片 4 列降为 2 列，状态卡片垂直堆叠，按钮全宽，Toast 占满宽度。420px 以下进一步降为单列。所有中文内容修复为正确的 UTF-8（之前 PowerShell 写入导致 `??` 乱码）。
- **status.html**：内核检测卡片在移动端垂直堆叠，操作按钮居中换行。修复全部中文乱码。
- **collect.html**：采集方式选择网格在移动端降为单列，评级筛选项换行，进度统计 4 列降为 2 列，定时任务表格添加横向滚动容器，新建任务弹窗限制 `max-width:90vw` 防溢出。
- **sync.html**：同步进度统计 4 列在移动端降为 2 列，文本域 font-size 提升到 16px 避免 iOS 自动缩放。
- **history.html**：表格包裹横向滚动容器。
- **database.html**：统计概览 5 列在移动端降为 2 列，表切换按钮栏支持横向滚动，搜索栏 flex-wrap。

### 文档与代码一致性

- **DESIGN.md vs 实际代码**存在多处偏差（DESIGN.md 是 v1.0 设计稿，代码已迭代到 v2 架构）。主要差异：
  - 配置文件路径：DESIGN.md 写的是 `config/doukhub.json`，实际代码用 `~/.doukhub/config.json`（三层架构：工具层/配置层/内核层）。
  - DESIGN.md 提到飞书"全局配置表"，实际代码已废弃，全局配置改用 JSON 文件。
  - DESIGN.md §8 的 API 清单缺少 v2 端点（`/api/sync/v2/*`、`/api/kernels/*`、`/api/database/*`、`/api/collect/v2/*`）。
  - DESIGN.md §9.1 的 `config_table_id` 在实际 Config 中已不存在。
  - DEVELOPMENT.md 中的三层架构、5 表 SQLite、SSE 进度等描述与代码一致，是当前权威文档。
- **建议**：DESIGN.md 标注为 "v1.0 设计稿（历史归档）"，以 DEVELOPMENT.md 为准。

### 测试验证

- 79 个单元测试全部通过（`pytest tests/`）。
- 6 个页面（status/sync/collect/history/database/settings）全部 HTTP 200，中文渲染正常，无乱码。
- `/api/status` 正确返回 feishu/ttd/xhs 连通性 + kernels 安装状态 + services + stats + jobs。

---

## 2026-07-10 内核检测与状态修复

### 新增功能

- **内核检测 UI**：状态页面新增"内核检测"卡片。TikTokDownloader / XHS-Downloader 源码未安装时显示警告 + "从 GitHub 下载"按钮，点击后自动 `git clone --depth 1` 并安装依赖。
- **API 端点**：`GET /api/kernels/status`、`POST /api/kernels/{name}/install`。
- **ServiceManager.install()**：从 GitHub 克隆内核源码 + 安装 requirements.txt。
- **DownloaderService.source_exists**：检测 `main.py` 是否存在判断内核是否已安装。

### Bug 修复

- **重复路由**：`/api/status` 在 main.py 中定义了两次，FastAPI 匹配第一个（只返回 services/stats/jobs），前端拿不到 feishu/ttd/xhs 字段导致状态卡片永远停在"检测中"。删除重复定义，合并为统一的 status 端点。
- **一键更新 undefined**：`ServiceManager.update()` 返回值缺少 `name` 字段，前端模板 `${r.name}` 显示 `undefined`。所有返回值补齐 `name`，并在内核未安装时给出明确提示。
- **start.bat 乱码**：PowerShell here-string 写入时中文变 `??`。改为纯 ASCII 英文 + 直接调用 `venv\Scripts\python.exe main.py`（绕开 activate 不生效导致加载全局 fastapi 的问题）。

---

## 2026-07-08 ~ 09 初始版本

- 项目骨架：FastAPI + Jinja2 + HTMX + APScheduler。
- 飞书 API 交互模块（httpx 封装，无 SDK 依赖）。
- 三步同步流程：导入采集表 → 更新采集表（获取 sec_user_id）→ 同步账号表。
- SQLite 本地数据库（5 张表，中文字段名）。
- SSE 实时进度 + 停止按钮（AbortController）。
- Cookie 轮换管理（随机/顺序，使用上限）。
- 定时任务（APScheduler + 数据库持久化）。
- 数据库管理界面（查看/删除/清空/搜索）。
 - 飞书双向同步（本地 ↔ 飞书）。
