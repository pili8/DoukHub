# DoukHub — 设计方案

> 基于 Web UI 的多平台内容采集管理工具
> 版本：v1.0 设计稿 | 日期：2026-07-08

---

## 1. 项目概述

### 1.1 定位

DoukHub 是一个**壳工具**，内核完全依赖两个独立项目：

- **TikTokDownloader** (抖音 + TikTok) — Web API 模式，端口 5555
- **XHS-Downloader** (小红书) — Web API 模式，端口 5556

DoukHub 不 import 任何内核代码，仅通过 HTTP API 调用。内核项目随时更新，壳不受影响。

### 1.2 核心设计原则

| 原则 | 说明 |
|------|------|
| API 消耗最小化 | 飞书 API、Downloader API 都尽量减少调用次数 |
| 飞书覆盖本地 | 飞书是数据唯一来源，本地只是断网保险，单向覆盖 |
| 壳核分离 | DoukHub 不依赖内核代码，仅通过 HTTP 交互 |
| 开箱即用 | 拿到 DoukHub + 配好飞书 App 即可使用 |

### 1.3 支持平台

| 平台 | 内核 | API 端口 |
|------|------|---------|
| 抖音 | TikTokDownloader | 5555 |
| TikTok | TikTokDownloader | 5555 |
| 小红书 | XHS-Downloader | 5556 |

---

## 2. 系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    飞书多维表格                        │
│              （配置中心 / 唯一数据来源）                  │
│  ┌──────────────┐  ┌──────────────┐                  │
│  │ 账号配置表    │  │ 全局配置表    │                  │
│  └──────────────┘  └──────────────┘                  │
└───────────────────────┬─────────────────────────────┘
                        │ 飞书 API（单向读取）
                        ▼
┌─────────────────────────────────────────────────────┐
│                   DoukHub（壳）                       │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ Web UI   │  │ 调度引擎  │  │ 本地缓存 (XLSX)  │  │
│  │(FastAPI + │  │(APScheduler│  │ 账号列表镜像     │  │
│  │ Jinja2 + │  │ 定时任务)  │  │                  │  │
│  │ HTMX)    │  │          │  └──────────────────┘  │
│  └────┬─────┘  └────┬─────┘  ┌──────────────────┐  │
│       │             │        │ 采集历史 (SQLite) │  │
│       │             │        └──────────────────┘  │
│       └──────┬──────┘                               │
│              │ HTTP API 调用                         │
└──────────────┼──────────────────────────────────────┘
               │
    ┌──────────┴──────────┐
    ▼                     ▼
┌─────────────┐   ┌─────────────┐
│TikTokDownloader│ │XHS-Downloader│
│ Web API :5555  │ │ Web API :5556│
│ (subprocess)   │ │ (subprocess)  │
└─────────────┘   └─────────────┘
    │                     │
    ▼                     ▼
  本地磁盘              本地磁盘
 (视频/图片/数据)     (视频/图片/数据)
```

### 2.2 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | Jinja2 + HTMX + 简单 CSS | 无需 Node.js，纯 Python 渲染 |
| 后端 | FastAPI + Uvicorn | 与内核技术栈一致 |
| 定时任务 | APScheduler | 内置调度器，不依赖系统 cron |
| 飞书交互 | httpx 调用飞书开放 API | 自行封装，不用飞书 SDK |
| 本地存储 | openpyxl (XLSX) + sqlite3 | 账号表用 XLSX，历史用 SQLite |
| 进程管理 | subprocess | 管理两个 Downloader API 服务 |

### 2.3 跨平台支持

- **Windows** — 主要开发和测试平台
- **Linux** — 服务器部署
- **Docker** — 提供 Dockerfile，一键部署

Web UI 天然跨平台，浏览器访问即可。

---

## 3. 飞书多维表格设计

### 3.1 账号配置表

| 字段名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| 账号名称 | 文本 | ✅ | — | 用户自定义备注名 |
| 平台 | 单选 | ✅ | — | 抖音 / TikTok / 小红书 |
| 链接 | URL | ✅ | — | 账号主页链接 |
| Cookie | 文本 | ✅ | — | 登录Cookie，支持多个（换行分隔），自动轮换 |
| 采集类型 | 单选 | ✅ | 发布 | 发布 / 喜欢 / 收藏 |
| 代理 | 文本 | ❌ | 空 | 代理地址，不填则不使用 |
| 启用 | 复选框 | ✅ | ✅ | 是否参与批量采集任务 |
| 评级 | 单选 | ✅ | ⭐⭐⭐ | ⭐ / ⭐⭐ / ⭐⭐⭐ / ⭐⭐⭐⭐ / ⭐⭐⭐⭐⭐ |
| 标签 | 多选 | ❌ | — | 自定义标签，如"竞品"、"KOL" |
| 备注 | 文本 | ❌ | — | 自由备注 |
| --- 以下为 DoukHub 自动回填 --- | | | | |
| sec_user_id | 文本 | 自动 | — | 首次同步时通过 API 获取 |
| 昵称 | 文本 | 自动 | — | 首次同步时通过 API 获取 |
| 粉丝数 | 数字 | 自动 | — | 首次同步时通过 API 获取 |
| 作品数 | 数字 | 自动 | — | 首次同步时通过 API 获取 |
| 签名 | 文本 | 自动 | — | 首次同步时通过 API 获取 |
| 头像 | URL | 自动 | — | 首次同步时通过 API 获取 |
| 同步时间 | 日期 | 自动 | — | 上次同步时间 |

**说明：**
- 用户只需填写前 10 个字段（手动填写部分）
- 后 7 个字段由 DoukHub 首次同步时自动获取并回填
- 已有账号信息的链接不再重复查询（省 API）
- 评级用于采集优先级排序和筛选

### 3.2 全局配置表

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| download_path | 文本 | D:\DoukHub\Download | 本地下载路径 |
| storage_format | 单选 | xlsx | 数据存储格式：csv / xlsx / sql |
| max_retry | 数字 | 5 | 请求重试次数 |
| timeout | 数字 | 10 | 请求超时（秒） |
| cookie_rotation_mode | 单选 | 随机 | Cookie轮换模式：随机 / 顺序 |
| cookie_usage_limit | 数字 | 10 | 单个Cookie使用次数上限，达到后轮换 |
| concurrent_accounts | 数字 | 3 | 并发采集账号数 |

**说明：**
- 此表为简单 KV 结构，两列：配置项 + 值
- 不常用的高级参数（chunk、name_format 等）直接改 Downloader 的 settings.json

---

## 4. 本地存储设计

### 4.1 账号列表缓存（XLSX）

路径：`DoukHub/data/accounts.xlsx`

- 飞书同步时全量覆盖此文件
- 结构与飞书账号配置表一致
- 飞书 API 不可用时，读取此文件继续运行
- 用户可手动打开查看和临时编辑（下次同步会被覆盖）

### 4.2 采集历史（SQLite）

路径：`DoukHub/data/history.db`

```sql
CREATE TABLE collection_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_name TEXT,          -- 账号名称
    platform TEXT,              -- 平台
    sec_user_id TEXT,           -- 账号唯一标识
    collection_type TEXT,       -- 采集类型（发布/喜欢/收藏）
    works_count INTEGER,        -- 采集到的作品数
    success_count INTEGER,      -- 成功下载数
    fail_count INTEGER,         -- 失败数
    started_at DATETIME,        -- 开始时间
    finished_at DATETIME,       -- 结束时间
    duration_seconds INTEGER,   -- 耗时
    status TEXT,                -- 完成/失败/中断
    error_message TEXT          -- 错误信息
);

CREATE TABLE scheduled_tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,                  -- 任务名称
    cron_expression TEXT,       -- cron 表达式
    rating_filter TEXT,         -- 评级筛选（如 "3,4,5"）
    enabled BOOLEAN,            -- 是否启用
    last_run_at DATETIME,       -- 上次执行时间
    next_run_at DATETIME,       -- 下次执行时间
    created_at DATETIME
);
```

### 4.3 目录结构

```
DoukHub/
├── app/                    # DoukHub 应用代码
│   ├── main.py             # 入口
│   ├── api/                # FastAPI 路由
│   ├── core/               # 核心逻辑
│   │   ├── feishu.py       # 飞书 API 交互
│   │   ├── syncer.py       # 同步引擎
│   │   ├── scheduler.py    # 定时任务调度
│   │   └── collector.py    # 采集调度
│   ├── services/           # Downloader 进程管理
│   │   ├── tiktok_downloader.py
│   │   └── xhs_downloader.py
│   ├── templates/          # Jinja2 模板
│   ├── static/             # CSS/JS
│   └── data/               # 本地数据
│       ├── accounts.xlsx   # 账号缓存
│       └── history.db      # 采集历史
├── config/                 # DoukHub 自身配置
│   └── doukhub.json        # 飞书 App 凭证等
├── TikTokDownloader/       # 内核（git submodule 或手动放置）
├── XHS-Downloader/         # 内核（git submodule 或手动放置）
├── requirements.txt
├── Dockerfile
└── DESIGN.md               # 本文档
```

---

## 5. 功能设计

### 5.1 服务管理

DoukHub 启动时自动拉起两个 Downloader API 服务：

| 服务 | 启动命令 | 端口 | 状态检测 |
|------|---------|------|---------|
| TikTokDownloader API | `python main.py` → 选择 Web API 模式 | 5555 | GET http://127.0.0.1:5555/settings |
| XHS-Downloader API | `python main.py API` | 5556 | GET http://127.0.0.1:5556/ |

**UI 上的交互：**
- 仪表盘显示两个服务的运行状态（🟢 运行中 / 🔴 已停止）
- 每个服务有独立的"启动"和"停止"按钮
- DoukHub 关闭时自动停止所有服务

### 5.2 同步引擎

#### 5.2.1 同步触发

- 手动：点击 Web UI 上的"同步"按钮
- 自动：每次启动 DoukHub 时自动同步一次

#### 5.2.2 同步流程

```
1. 读取飞书账号配置表全部数据
2. 过滤出"链接不为空"的记录
3. 识别新增记录（没有 sec_user_id 的）
4. 对新增记录逐个调用 Downloader API 获取账号信息：
   - 抖音/TikTok → POST http://127.0.0.1:5555/douyin/account 或 /tiktok/account
   - 小红书 → POST http://127.0.0.1:5556/xhs/detail
5. 获取到 sec_user_id 后，与已有账号列表比对：
   - 重复 → 在 Web UI 弹出提醒，让用户选择处理方式
   - 不重复 → 将账号信息回填到飞书表 + 更新本地缓存
6. 全量覆盖本地 accounts.xlsx
```

#### 5.2.3 重复处理 UI

```
┌─────────────────────────────────────────────┐
│  ⚠️ 发现重复账号                              │
│                                             │
│  新增账号："竞品A" (抖音)                      │
│  链接：https://www.douyin.com/user/ABC123     │
│  sec_user_id：SEC_ABC123                     │
│                                             │
│  与以下已有账号重复：                           │
│  ┌─────────────────────────────────────────┐ │
│  │ 账号名称：竞品A-旧                        │ │
│  │ 评级：⭐⭐                               │ │
│  │ 标签：竞品                               │ │
│  └─────────────────────────────────────────┘ │
│                                             │
│  [保留新条目，删除旧条目]                       │
│  [保留旧条目，删除新条目]                       │
│  [两条都保留（标记重复）]                       │
│  [跳过，稍后处理]                              │
└─────────────────────────────────────────────┘
```

### 5.3 账号管理

- **不在 DoukHub 中增删改账号** — 账号管理在飞书上完成
- DoukHub 只负责：
  - 展示从飞书同步过来的账号列表（表格形式）
  - 按平台、评级、标签、启用状态筛选
  - 显示账号基础信息（昵称、粉丝数等）
  - 对单个账号触发"立即采集"

### 5.4 采集功能

#### 5.4.1 整号采集

**触发方式：**
- 从账号列表中选择一个或多个账号 → 点击"采集"
- 或从定时任务自动触发

**参数：**

| 参数 | 来源 | 说明 |
|------|------|------|
| sec_user_id | 账号列表 | 自动读取 |
| 平台 | 账号列表 | 自动判断调用哪个 API |
| Cookie | 账号列表 | 支持多个，自动轮换 |
| 采集类型 | 账号列表 | 发布/喜欢/收藏 |
| 代理 | 账号列表 | 可选 |
| 评级筛选 | 用户选择 | 只采集指定星级以上的账号 |
| 日期范围 | 用户选择 | 可选，筛选作品发布时间 |
| 并发数 | 全局配置 | 同时采集几个账号，默认3 |

**API 调用：**

```
抖音: POST http://127.0.0.1:5555/douyin/account
{
    "sec_user_id": "SEC_xxx",
    "cookie": "...",
    "tab": "post",          // post / favorite / collection
    "earliest": "2024-01-01",
    "latest": "2026-07-08",
    "source": false
}

TikTok: POST http://127.0.0.1:5555/tiktok/account
{
    "sec_user_id": "SEC_xxx",
    "cookie": "...",
    "tab": "post"
}

小红书: POST http://127.0.0.1:5556/xhs/detail
{
    "url": "https://www.xiaohongshu.com/user/profile/xxx",
    "download": true
}
```

**Cookie 轮换机制：**
- 每个账号可配置多个 Cookie（换行分隔）
- 轮换模式：随机 / 顺序
- 单个 Cookie 使用次数上限（默认10次），达到后自动切换
- 目的：分散请求，降低风控风险

**并发采集机制：**
- 默认同时采集3个账号（可在设置中调整）
- 组内并发，组间加入随机延时
- 并发数不宜过高，避免触发平台风控

**执行流程：**
```
1. 按评级排序（高星级优先）
2. 按并发数分组（默认3个一组）
3. 每组内并发调用 API，组间加入随机延时
4. Cookie 从池中随机/顺序选取，达到使用上限后轮换
5. 记录执行结果到 history.db
6. 失败的账号标记并继续下一个
```

#### 5.4.2 单品采集

**触发方式：**
- 在 Web UI 的输入框中粘贴链接（1~N 个）
- 系统自动识别链接数量和平台

**输入框设计：**
```
┌─────────────────────────────────────────────┐
│  粘贴作品链接（支持多个，自动识别）             │
│  ┌─────────────────────────────────────────┐ │
│  │ https://www.douyin.com/video/123456     │ │
│  │ https://www.douyin.com/video/789012     │ │
│  │ https://www.xiaohongshu.com/explore/... │ │
│  └─────────────────────────────────────────┘ │
│                                             │
│  识别到：抖音 2 个，小红书 1 个                │
│                                             │
│  [开始采集]                                   │
└─────────────────────────────────────────────┘
```

**API 调用：**

```
抖音单品: POST http://127.0.0.1:5555/douyin/detail
{
    "detail_id": "1234567890123456789",
    "cookie": "..."
}

小红书单品: POST http://127.0.0.1:5556/xhs/detail
{
    "url": "https://www.xiaohongshu.com/explore/xxx",
    "download": true
}
```

**执行流程：**
```
1. 解析输入框内容，提取所有有效链接
2. 按平台分组（抖音/TikTok 一组，小红书一组）
3. 逐个调用对应 API
4. 记录结果
5. UI 实时显示进度
```

### 5.5 定时任务

#### 5.5.1 配置

| 参数 | 说明 |
|------|------|
| 任务名称 | 自定义名称 |
| Cron 表达式 | 如 `0 2 * * *`（每天凌晨2点） |
| 评级筛选 | 只采集哪些星级的账号 |
| 启用/禁用 | 开关 |

#### 5.5.2 执行逻辑

```
1. 定时器触发
2. 读取本地 accounts.xlsx（此时不调飞书 API）
3. 筛选"启用"的 + 符合评级条件的账号
4. 按评级排序，高优先
5. 逐个调用 Downloader API 采集
6. 记录结果到 history.db
```

**注意：定时任务运行时读本地缓存，不读飞书。** 避免定时任务因飞书 API 故障而失败。

### 5.6 任务记录

- 展示采集历史（从 history.db 读取）
- 字段：时间、账号、平台、采集类型、成功数、失败数、耗时、状态
- 支持按时间、账号、状态筛选
- 纯本地数据，不走飞书

---

## 6. Web UI 页面设计

### 6.1 页面结构

```
┌─────────────────────────────────────────────┐
│  DoukHub                           [设置]    │
├─────────┬───────────────────────────────────┤
│         │                                   │
│  📊 仪表盘 │         主内容区域               │
│  👤 账号   │                                  │
│  📥 采集   │                                  │
│  ⏰ 定时   │                                  │
│  📋 记录   │                                  │
│  ⚙️ 设置   │                                  │
│         │                                   │
└─────────┴───────────────────────────────────┘
```

### 6.2 仪表盘

```
┌─────────────────────────────────────────────┐
│  服务状态                                     │
│  ┌──────────────┐  ┌──────────────┐          │
│  │ TikTokDownloader│ │ XHS-Downloader│         │
│  │ 🟢 运行中     │  │ 🟢 运行中    │          │
│  │ [:5555]      │  │ [:5556]     │          │
│  │ [停止]        │  │ [停止]       │          │
│  └──────────────┘  └──────────────┘          │
│                                             │
│  快速统计                                     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐        │
│  │ 账号  │ │ 今日  │ │ 成功  │ │ 失败  │        │
│  │ 128  │ │  5   │ │  4   │ │  1   │        │
│  └──────┘ └──────┘ └──────┘ └──────┘        │
│                                             │
│  [同步飞书]  [全部采集]                        │
└─────────────────────────────────────────────┘
```

### 6.3 账号页面

```
┌─────────────────────────────────────────────┐
│  筛选：[全部平台 ▾] [全部评级 ▾] [全部标签 ▾]  │
│  搜索：[________________]  [同步飞书]          │
│                                             │
│  ┌──┬──────┬──────┬────┬────┬────┬────┐     │
│  │✓ │名称   │平台   │评级 │粉丝  │作品  │启用 │     │
│  ├──┼──────┼──────┼────┼────┼────┼────┤     │
│  │☐ │竞品A  │抖音   │⭐⭐⭐│12万 │356 │🟢 │     │
│  │☐ │KOL-B │小红书 │⭐⭐⭐⭐│8万  │89  │🟢 │     │
│  │☐ │竞品C  │抖音   │⭐⭐  │3万  │120 │🔴 │     │
│  └──┴──────┴──────┴────┴────┴────┴────┘     │
│                                             │
│  选中后：[采集选中] [采集全部启用的]             │
└─────────────────────────────────────────────┘
```

### 6.4 采集页面

```
┌─────────────────────────────────────────────┐
│  整号采集                                     │
│  评级筛选：[✓⭐⭐⭐] [✓⭐⭐⭐⭐] [☐⭐⭐] [☐⭐]     │
│  日期范围：[________] 至 [________]（可选）     │
│  [开始整号采集]                                │
│                                             │
│  ───────────────────────────────────────     │
│                                             │
│  单品采集                                     │
│  ┌─────────────────────────────────────────┐ │
│  │ 粘贴作品链接（支持多个，自动识别）         │ │
│  │                                         │ │
│  └─────────────────────────────────────────┘ │
│  识别到：抖音 0 个，TikTok 0 个，小红书 0 个   │
│  [开始单品采集]                                │
│                                             │
│  ───────────────────────────────────────     │
│                                             │
│  执行进度                                     │
│  竞品A (抖音)  ████████████░░░  80%  进行中    │
│  KOL-B (小红书) ████████████████ 100%  完成    │
└─────────────────────────────────────────────┘
```

### 6.5 定时任务页面

```
┌─────────────────────────────────────────────┐
│  [+ 新建定时任务]                              │
│                                             │
│  ┌─────────────────────────────────────────┐ │
│  │ 每日巡检-竞品                             │ │
│  │ Cron: 0 2 * * *（每天凌晨2点）           │ │
│  │ 评级筛选：⭐⭐⭐ 以上                      │ │
│  │ 状态：🟢 启用                             │ │
│  │ 上次执行：2026-07-08 02:00  成功 4/5      │ │
│  │ [编辑] [禁用] [立即执行] [删除]            │ │
│  └─────────────────────────────────────────┘ │
│                                             │
│  ┌─────────────────────────────────────────┐ │
│  │ 周末全量采集                              │ │
│  │ Cron: 0 3 * * 6,0（周六日凌晨3点）       │ │
│  │ 评级筛选：全部                             │ │
│  │ 状态：🔴 禁用                             │ │
│  │ [编辑] [启用] [立即执行] [删除]            │ │
│  └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 6.6 记录页面

```
┌─────────────────────────────────────────────┐
│  筛选：[全部状态 ▾] [________] 至 [________]  │
│                                             │
│  ┌──────┬──────┬──────┬────┬────┬────┬────┐ │
│  │时间   │账号   │平台   │类型 │成功  │失败 │耗时│ │
│  ├──────┼──────┼──────┼────┼────┼────┼────┤ │
│  │07-08 │竞品A  │抖音   │发布 │356  │0   │12m│ │
│  │07-08 │KOL-B  │小红书 │发布 │89   │2   │5m │ │
│  │07-07 │竞品A  │抖音   │发布 │350  │0   │11m│ │
│  └──────┴──────┴──────┴────┴────┴────┴────┘ │
└─────────────────────────────────────────────┘
```

### 6.7 设置页面

```
┌─────────────────────────────────────────────┐
│  飞书配置                                     │
│  App ID:      [________________________]     │
│  App Secret:  [________________________]     │
│  表格 URL:    [________________________]     │
│  [测试连接]                                   │
│                                             │
│  ───────────────────────────────────────     │
│                                             │
│  下载设置                                     │
│  下载路径:    [D:\DoukHub\Download     ] [..]│
│  存储格式:    (●) XLSX  ( ) CSV  ( ) SQL     │
│                                             │
│  ───────────────────────────────────────     │
│                                             │
│  服务配置                                     │
│  TTD 路径:    [./TikTokDownloader      ] [..]│
│  XHS 路径:    [./XHS-Downloader         ] [..]│
│  TTD 端口:    [5555]                         │
│  XHS 端口:    [5556]                         │
│                                             │
│  [保存设置]                                   │
└─────────────────────────────────────────────┘
```

---

## 7. 核心流程

### 7.1 首次使用流程

```
1. 下载 DoukHub + 放入 TikTokDownloader 和 XHS-Downloader
2. 启动 DoukHub → 浏览器打开 Web UI
3. 进入设置页 → 填写飞书 App ID / Secret / 表格 URL
4. 点击"测试连接" → 连接成功
5. 进入账号页 → 点击"同步飞书" → 读取账号列表
6. 对新账号自动获取账号信息并回填
7. 处理重复账号（如有）
8. 进入采集页 → 选择账号 → 开始采集
```

### 7.2 日常使用流程

```
1. 在飞书上添加新账号（只填链接和基本信息）
2. 打开 DoukHub → 同步 → 新账号自动获取信息
3. 手动触发采集 或 等待定时任务自动执行
4. 查看采集记录
5. 本地查看下载的文件
```

### 7.3 定时巡检流程

```
1. DoukHub 启动 → APScheduler 加载定时任务
2. 到达触发时间 → 读取本地 accounts.xlsx
3. 按评级筛选 → 排序 → 逐个采集
4. 结果写入 history.db
5. DoukHub 保持运行，等待下次触发
```

---

## 8. API 接口清单

### 8.1 DoukHub 自身 Web API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | / | Web UI 主页 |
| GET | /api/status | 服务状态（两个 Downloader 是否运行） |
| POST | /api/sync | 触发飞书同步 |
| GET | /api/accounts | 获取本地账号列表 |
| POST | /api/collect/account | 触发整号采集 |
| POST | /api/collect/detail | 触发单品采集 |
| GET | /api/history | 获取采集历史 |
| GET | /api/schedules | 获取定时任务列表 |
| POST | /api/schedules | 创建定时任务 |
| PUT | /api/schedules/{id} | 更新定时任务 |
| DELETE | /api/schedules/{id} | 删除定时任务 |
| POST | /api/schedules/{id}/run | 立即执行定时任务 |
| GET | /api/settings | 获取设置 |
| PUT | /api/settings | 更新设置 |
| POST | /api/services/{name}/start | 启动指定 Downloader 服务 |
| POST | /api/services/{name}/stop | 停止指定 Downloader 服务 |

### 8.2 调用的外部 API

| 目标 | 端点 | 用途 |
|------|------|------|
| TTD | POST /douyin/account | 抖音账号作品采集 |
| TTD | POST /douyin/detail | 抖音单品采集 |
| TTD | POST /douyin/mix | 抖音合集采集 |
| TTD | POST /tiktok/account | TikTok 账号作品采集 |
| TTD | POST /tiktok/detail | TikTok 单品采集 |
| TTD | GET /settings | 获取 TTD 配置 |
| TTD | POST /settings | 更新 TTD 配置 |
| XHS | POST /xhs/detail | 小红书单品/账号采集 |
| 飞书 | GET /open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records | 读取多维表格 |
| 飞书 | PUT /open-apis/bitable/v1/apps/{app_token}/tables/{table_id}/records/{record_id} | 更新记录 |
| 飞书 | POST /open-apis/auth/v3/tenant_access_token/internal | 获取飞书 access token |

---

## 9. 配置文件

### 9.1 doukhub.json

DoukHub 自身的配置，存放在 `DoukHub/config/doukhub.json`：

```json
{
    "feishu": {
        "app_id": "",
        "app_secret": "",
        "app_token": "",
        "account_table_id": "",
        "config_table_id": ""
    },
    "downloader": {
        "tiktok_downloader_path": "./TikTokDownloader",
        "xhs_downloader_path": "./XHS-Downloader",
        "ttd_port": 5555,
        "xhs_port": 5556,
        "auto_start_services": true
    },
    "local": {
        "download_path": "D:\\DoukHub\\Download",
        "storage_format": "xlsx",
        "data_dir": "./data"
    },
    "cookie": {
        "rotation_mode": "random",
        "usage_limit": 10
    },
    "concurrent_accounts": 3
}
```

---

## 10. 安全考虑

| 风险 | 措施 |
|------|------|
| Cookie 泄露 | Cookie 仅存在飞书表和本地缓存中，不写入日志 |
| 飞书凭证泄露 | doukhub.json 不纳入 git，.gitignore 排除 |
| API 滥用 | Web UI 默认仅监听 127.0.0.1，远程访问需配置 |
| 文件安全 | 下载路径不做自动清理，用户自行管理磁盘空间 |

---

## 11. 开发计划

### Phase 1：核心骨架
- [ ] DoukHub 项目结构搭建
- [ ] FastAPI + Jinja2 + HTMX 基础框架
- [ ] 飞书 API 交互模块
- [ ] Downloader 进程管理（subprocess 启停）
- [ ] 配置文件读写

### Phase 2：同步与账号
- [ ] 飞书同步引擎（读取 → 获取信息 → 回填 → 覆盖本地）
- [ ] 账号列表页面（展示、筛选）
- [ ] 重复账号检测与处理 UI

### Phase 3：采集功能
- [ ] 整号采集（调用 TTD/XHS API）
- [ ] 单品采集（链接解析 + 分平台调用）
- [ ] 采集进度实时展示
- [ ] 采集历史记录

### Phase 4：定时任务
- [ ] APScheduler 集成
- [ ] 定时任务 CRUD
- [ ] 评级筛选逻辑

### Phase 5：完善
- [ ] Docker 支持
- [ ] 错误处理与重试
- [ ] 日志系统
- [ ] UI 美化

---

## 12. 待确认事项

> 以下事项在开发过程中可能需要调整：

1. 飞书多维表格的实际字段名称（需与用户确认后适配）
2. 小红书批量采集的效率问题（XHS-Downloader API 只有单个端点）
3. 定时任务运行时是否需要自动同步飞书（当前设计为不自动同步，避免 API 消耗）
4. 多个 DoukHub 实例是否可以同时运行（当前设计不支持，需要单实例）

---

## 13. UI 设计准则与视觉规范（Design System）

> 本章为前端视觉与交互的**长期准则**。新增 / 修改任何 UI 前先阅读，保持全局一致。
> 版本：v1 | 2026-08-18 | 由单作品采集页改造实践沉淀

### 13.1 核心原则

| 原则 | 说明 |
|------|------|
| **全 token 化，禁止硬编码颜色** | 所有颜色一律用 `var(--dh-*)`，不得写死色值（硬编码会导致深浅主题不跟随、多页不统一，历史教训：日志框 `#1a1c1e` 硬编码） |
| **深浅主题自动跟随** | 项目有完整暗色模式（`data-theme` 切换 + localStorage 记忆 + 跟随系统），用 token 即自动适配，零额外代码 |
| **风格统一优先** | 新组件先对齐项目已有范式（按钮 6px 圆角、卡片白底细边框、胶囊容器），不引入跳色 |
| **自用工具，精简文字** | 说明文字只留必要提示，删冗长解释 |
| **层次用填充强度表达** | 主次用 Filled / Tonal / Ghost 或浅深两档蓝，不用不同色相跳色 |

### 13.2 主题机制

- 控制方式：`<html data-theme="light|dark">`（`style.css` 中 `:root` 与 `:root[data-theme="dark"]` 分别定义全部 token）
- 切换逻辑：`base.html` 内联脚本——`localStorage` 记忆用户选择，无记忆时跟随系统 `prefers-color-scheme`
- **规则：写样式永远引用 `var(--dh-*)`，让主题自动切换生效**

### 13.3 配色体系（浅色模式 · :root）

| Token | 色值 | 角色 |
|-------|------|------|
| `--dh-background` | `#FBFAF7` | 页面背景（暖白） |
| `--dh-surface` | `#FFFFFF` | 卡片 / 面板表面 |
| `--dh-surface-muted` | `#F5F2EC` | 次级容器 / 悬停反馈 / 胶囊底 / 代码预览（次级表面色） |
| `--dh-accent` | `#0061A4` | 主色（蓝），主按钮 / 链接 / 强调 |
| `--dh-accent-soft` | `#E7F0F8` | 主色浅底（筛选按钮高亮 / tonal 按钮） |
| `--dh-border` | `#ECE7DF` | 常规边框（暖灰） |
| `--dh-border-strong` | `#D9D3C8` | 强边框 / 分隔 |
| `--dh-text` / `--dh-text-secondary` / `--dh-text-muted` | `#1F1B16` / `#5C564D` / `#9C9387` | 主文字 / 次级 / 弱化 |

### 13.4 配色体系（深色模式 · :root[data-theme="dark"]）

| Token | 色值 | 角色 |
|-------|------|------|
| `--dh-background` | `#17130F` | 页面背景（深棕黑） |
| `--dh-surface` | `#211C17` | 卡片表面 |
| `--dh-surface-muted` | `#2A241D` | 次级容器 / 悬停 |
| `--dh-accent` | `#4DA3FF` | 主色（亮蓝） |
| `--dh-accent-soft` | `rgba(77,163,255,.16)` | 主色浅底 |
| `--dh-border` / `--dh-border-strong` | `#3D352C` / `#4F463A` | 边框 |
| 文字三档 | `#F7F3EC` / `#D2C9BE` / `#9C9387` | 主 / 次级 / 弱化 |

### 13.5 语义色（状态 / 进度场景）

| 语义 | Token | 浅色 | 深色 | 用途 |
|------|-------|------|------|------|
| 进行中 / 信息 | `--dh-accent` | 蓝 | 亮蓝 | 日志 info、进度条、计数卡"总数" |
| 成功 | `--dh-success` | 绿 | 亮绿 | 日志 ok、分段条完成段、计数卡"成功" |
| 失败 | `--dh-danger` | 红 | 亮红 | 日志 err、分段条失败段、计数卡"失败" |
| 警告 | `--dh-warning` | 橙 | 亮黄 | 日志 warn、警告提示 |

**统一约定：进度 / 状态场景下，蓝=进行中、绿=成功、红=失败、橙=警告，全项目一致。**

### 13.6 组件范式

| 组件 | 规范 |
|------|------|
| 按钮 | `.btn`：6px 圆角、34px 高。主=`.btn-primary` 实心蓝、次=`.btn-secondary` 白底描边、弱化=ghost 虚线框灰字（仅展示入口） |
| 胶囊容器 | `border-radius: var(--radius-full)`（三模式按钮组、下载方式 tab、平台徽章） |
| 卡片 | `.workflow-panel`（可叠加 `.collect-panel`）：白底 + 1px 细边框 + 6px 圆角 + 20px 内边距，卡片间距 16px |
| 次级容器 | 需要与卡片区分的容器（日志框、代码块、胶囊底）用 `--dh-surface-muted` |
| 折叠交互 | 折叠头 + chevron（旋转 90° 表示展开）；用于：下载设置、低频资产组、下载历史、模板弹层 |
| 状态卡片块 | `.workflow-metric`：左边条 + 圆角卡片，内部「标签 数字」横排（数字加粗带语义色） |
| 高亮层次 | 筛选按钮=`accent-soft` 浅蓝底（弱于主按钮）；主操作=实心蓝。**同一场景只高亮一个主打项**（如主打类型筛选：视频 > 实况 > 图片） |
| 资产列表 | 白底 + 行间细分隔线（不用每行底色），低频项折叠分组、展开后正常样式 |
| 进度日志框 | 浅色（`surface-muted` 底 + 语义色行）+ 等宽字体保留"终端感"；**不用深色黑底** |

### 13.7 布局与间距

- 基准间距 16px；卡片内边距 20px；卡内区块间距 16px
- 单作品采集页固定四卡片结构：**作品链接 → 进度 → 资产结果 → 下载历史**
- 输入框操作按钮（粘贴 / 清空）悬浮于右下角，不占独立行
- 资产列表行高紧凑（缩略图 40px），超出限高滚动

### 13.8 交互惯例

- **输入即反馈**：链接输入后即时预检，统计条一行汇总（数量 · 匹配数 · 平台计数），无效链接可点击定位
- **模式并列**：同一位置三按钮表示三种模式（解析 / 一键下载 / API），按钮动作 + 设置项联动
- **低频内容折叠**：音乐 / 封面等低频资产收进折叠组，默认收起只占一行
- **进度流式展示**：分段彩色进度条（完成=绿 / 进行中=蓝 / 失败=红 / 未到=灰）+ 当前消息 + 计数卡 + 日志行；解析=2 段、下载=3 段
- **下载方式**：本地 / 浏览器选择放在下载动作时弹出（不放在设置里）

### 13.9 进度日志浅色化记录

> ✅ **已于 2026-08-18 全部完成**：全局 `.workflow-log` 已改为 token 浅色（`--dh-surface-muted` 底 + 语义色行），所有页面自动生效；cloud.html 内联深色样式（`.sync-log-box` / `.history-log`，`#0f172a`）已删除并归一；style.css 未使用的 `.log-box` 也一并 token 化。保留以下清单作为改造历史记录：

1. `collect.html:413` — `<pre class="workflow-log">`（批量采集流程日志）
2. `sync/resolve.html` — `#exec-log`（解析分享表）
3. `sync/account.html` — `#exec-log`（生成账号表）
4. `sync/refresh.html` — `#exec-log`（更新账号表）
5. `sync/import.html` — `#exec-log`（导入分享表）
6. `sync/cloud.html` — 内联 `.history-log` / `.sync-log-box`（`#0f172a` 第二套硬编码色值，已删内联归一到统一 class）
7. `_workflow.html` — `.history-log`（sync 历史任务日志）

统一方式：改全局 `.workflow-log` 为 token 浅色（各页 id 覆盖 `#sse-log` 已随全局统一而移除），并清理 cloud.html 内联深色样式。

## 14. 设置页结构（2026-08-21 定稿）

> 「整体设置页面」采用**分组 Tab + 折叠卡**，不做长滚动页。本节为结构决策记录。

### 14.1 分组 Tab 导航

- 顶部**分组 Tab**（DoukHub配置 / 下载与文件 / 内核服务 / 功能配置），点击切换面板（`.settings-group.active` 显隐），**不再滚动长页**。
- 组内仍可折叠卡（`.settings-card.open`）。
- 保留 `scrollToGroup` 兼容旧入口；支持 URL hash 直达分组（`#group-download`），供采集页「管理模板」等外部跳转。
- 深色主题 / 交互惯例沿用 §13 全 token 规范。

### 14.2 下载与文件分组结构

| 卡片 | 内容 |
|------|------|
| **保存方案** | 双卡横排：**增量采集**（按账号归档，根目录+平台/账号自动层级） / **单作品采集**（直存，默认目录留空则用增量根目录）；各卡含「编辑命名」直达统一编辑器 |
| **文件命名规则** | 统一命名编辑器：增量模板 / 单作品模板 小胶囊切换；字段**拉平** |
| **下载内容选项** | 增量附属下载（音乐/封面/大小上限/导出格式）；单作品附带下载在采集页「一键下载」设置 |

### 14.3 命名统一（字段拉平）

两套引擎字段**在 UI 层合并为同一套**，保存时按方案映射回引擎字段：

| UI 字段 | 增量引擎键 | 单作品引擎键 |
|---------|-----------|-------------|
| 发布时间 | `create_time` | `create_time` |
| 作品类型 | `type` | `type` |
| 作者 | `nickname` | `author` |
| 标题 | `desc` | `title` |
| 作品ID | `id` | `id` |
| 账号UID | `uid` | —（仅增量，角标） |
| 账号标识 | `mark` | —（仅增量，角标） |
| 平台 | —（仅单作品，角标） | `platform` |

- 编辑器同一套 chips，非当前方案可用字段自动隐藏，专属字段带「仅增量 / 仅单作品」角标。
- 单作品模板支持多模板 + 默认模板 + 「复制为增量」；字段编辑自动同步回当前模板并防抖持久化。
- **兼容性**：只改了前端 UI 与映射，后端 API（`/api/collection/defaults` 存 `name_format`、`/api/collection/single-work/preferences` 存 `{author}/{title}`）未变，存量配置零迁移。

### 14.4 共享命名工作台（2026-08-21 晚定稿 v6 → 落地）

> 用户批准 v6 布局原型，落地后的真实结构：**卡片内不再有命名编辑器**，统一收进**底部滑出式共享命名工作台**（单一 DOM 实例）。

#### 14.4.1 卡片区（两大组：单作品采集 / 增量采集）

| 组 | 内容 |
|----|------|
| 单作品采集 | 「保存目录」分区（方案目录 + 浏览，留空用增量根目录）+「命名」分区（**摘要行** + 「编辑模板」按钮，紫色强调） |
| 增量采集 | 「下载目录」分区（根目录 + 浏览 + 平台/账号目录树）+「命名」分区（摘要行 + 编辑模板，绿色强调）+「附属下载」分区（2 行格：第 1 行 音乐/动态封面/静态封面 开关；第 2 行 **文件大小上限在前**、**数据导出格式在后**） |

- 摘要行实时显示字段示例 + `.mp4`，字段顺序改动即时同步；「每作品独立子文件夹」开关已从批量卡移入工作台。

#### 14.4.2 底部共享命名工作台（`.workbench`，单一 DOM 实例）

- 触发：任一卡片「编辑模板」→ `openWorkbench('single'|'batch')`；遮罩点击 / 关闭按钮收起。
- 结构自上而下：
  1. **作用域胶囊**（单作品 / 增量）：`wbScope(s)` 切换，读写各自独立的 `tplState[scope].parts`；
  2. **模板管理条**（`#wb-tpl-sel`）：单作品=方案下拉（含默认标记）+ 新建 / 重命名 / ★设默认 / 删除 / 「设为增量默认」（`dupToBatch`，复制后自动切到增量作用域展示结果）；增量=固定「增量默认模板」占位（引擎级，无多方案）；
  3. **字段池**（`#wb-pool`）：chips 点击加入/移除命名，专属字段带「仅增量 / 仅单作品」角标；
  4. **排序带**（`#wb-belt`）：已选字段拖动换位、✕ 移除；
  5. **存储**（仅增量作用域显示）：「每作品独立子文件夹」开关（唯一保留的引擎级选项，`folder_mode`）；
  6. **预览**（`#wb-pv`）：文件名实时示例（分隔符 `-` 已固化）。
- JS 契约：`renderTplEditor(scope)` 幂等（摘要常驻 + 面板只渲染当前作用域）；`templateDragStart(event, scope, index)` 拖动签名修正了旧版参数错位；保存映射与 §14.3 一致，后端 API 未变。
